name: Secret Scan

on:
  pull_request:
    branches:
      - master

jobs:
  trufflehog:
    permissions:
      id-token: write
      contents: read
      issues: write
      pull-requests: write
    uses: RTBHouse-OEET/common-actions/.github/workflows/secret-scan.yml@master
